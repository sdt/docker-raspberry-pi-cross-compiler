#!/bin/sh

set -e

if [ $# -eq 0 ]; then
    echo usage: install-raspbian [--update] package [packages...]
    exit 1
fi

if [ "$1" = "--update" ]; then
    shift
    rpi-run apt-get update
fi

rpi-run DEBIAN_FRONTEND=noninteractive apt-get install -y "$@"
rpi-run symlinks -cors /
